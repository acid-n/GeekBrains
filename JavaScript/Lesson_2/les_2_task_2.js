'use sctrict';

// переменной "а" присваиваем число 2
let a = 2;
// переменной "х" присваиваем результат вычислений:
// в скобках вычисляем умножение переменной "а" на 2 (a *= 2, это тоже самое, что a = a * 2), получаем 4
// число 1 прибавляем к 4 и получаем 5, заносим в переменную "х"
let x = 1 + (a *= 2);
